# Вопросы к разработчику

2. Что происходит с `Position` и `Role` при вызове `DeletePerson`? Можно ли предотвратить удаление связанных сущностей?
2. Как именно устроен `IFilter` в `SearchPersons`: какие поля поддерживаются и есть ли примеры типовых запросов?
3. Есть ли официальный способ «очистить» должность (`Position`) без удаления сотрудника (например, отдельный метод `DeletePosition`), или достаточно упоминания о передаче пустого `Title`?
4. Может ли существовать несколько `Role` одинакового `RoleType` в одной организации (например, несколько уполномоченных представителей), или логика строго ограничивает одну запись на пару `Organization + RoleType`?
5. Как интерпретировать параметр `user` во всех методах: это всегда текущий (выбранный) пользователь, или сервис допускает особые сценарии, когда один системный пользователь (например, администратор или уполномоченное лицо организации) управляет данными других?
6. Что за "СОП"?
7. Предусматривается ли перевод статьи по сервису на английский или другие языки?